# dependencies
node_modules/

# Porthole runtime state. Holds the admin token, invite tokens and VAPID keys.
# Must never be committed.
.porthole/


docs/

# android build output and machine-local sdk paths
android/build/
android/app/build/
android/.gradle/
android/local.properties
# Release signing. The keystore itself lives in ~/.porthole/, never in the repo.
android/keystore.properties
*.jks
*.keystore
*.apk
*.aab

# logs and scratch
*.log
npm-debug.log*
.env
.env.*

# os cruft
Thumbs.db
desktop.ini
.DS_Store
