org.gradle.jvmargs=-Xmx2560m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# The system JDK on this machine is 26, which the Android Gradle Plugin rejects.
# Android Studio ships a JDK 21, which is what AGP expects. Without this the build
# fails with an unsupported class file version that reads like a corrupt dependency.
org.gradle.java.home=C\:\\Program Files\\Android\\Android Studio\\jbr

android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
